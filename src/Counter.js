/*import React, { useState } from "react";

const Counter = () => {
  const [count, setCount] = useState(0);
 

  const increment = () => {
    setCount(count + 1);
  };

  const decrement = () => {
    setCount(count - 1);
  };
  

  

  return (
    <div style={{ display: "flex", justifyContent: "center" }}>
      <div>
        <h2>This is Counter App</h2>
        <p style={{ fontSize: 50 }}>{count}</p>
        <button
          onClick={increment}
          style={{ fontsize: 30, marginRight: 10, padding: 10}} >+</button>
        <button
          onClick={decrement}
          style={{ fontsize: 30, marginRight: 10, padding: 10 }}>-</button>
        <div>
          </div>
      </div>
    </div>
  );
};
export default Counter;*/